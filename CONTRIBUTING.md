# 贡献指南

昆仑智策按 8 阶段、98 个原子节点推进开发。本文档概述协作流程，详细规则见 [AGENTS.md](./AGENTS.md)。

## 单节点工作流

1. 从实施手册中选取一个节点（P{phase}-N{sequence}）。
2. 阅读节点卡片及其前置依赖。
3. 以 TDD 方式实现：先写失败的测试，再实现最小方案。
4. 运行格式检查、静态分析、窄测试、受影响包测试。
5. 更新合同、文档和运维说明。
6. 提交并等待 Codex 审核。
7. 审核通过后才可开始下一节点。

## 提交约定

遵循 [Conventional Commits](https://www.conventionalcommits.org/)：

```
feat(scope): brief description

Node: P0-N01
```

提交范围聚焦单一节点，不混入无关格式、依赖升级和重构。

## 代码审核

Codex 审核关注需求符合性、架构边界、类型安全、测试充分性、安全、跨平台行为和操作完备性。审核未通过的节点不得合并。

## 关键原则

- **测试先行**：每个节点覆盖正常路径、边界条件和失败路径。
- **合同优先**：接口和事件模型先定义合同，再生产者和消费者。
- **数据安全**：Phase 0–6 不存在能够产生真实券商订单的调用路径。
- **跨平台**：开发、测试和脚本须兼容 Windows、macOS、Linux。
