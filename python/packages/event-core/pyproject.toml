[project]
name = "event-core"
version = "0.0.0"
description = "News/event raw content contract and source policy"
requires-python = "==3.12.13"
dependencies = [
    "ashare-contracts",
]

[tool.uv.sources]
ashare-contracts = { workspace = true }

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"
