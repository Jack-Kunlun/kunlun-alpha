[project]
name = "ashare-contracts"
version = "0.0.0"
description = "A-Share Python domain contracts (generated from JSON Schema)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pydantic>=2.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
